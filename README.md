
BCM Trading Telegram Result Generator

Deploy on Render

Build Command:
pip install -r requirements.txt

Start Command:
gunicorn --bind 0.0.0.0:$PORT app:app

Usage:
Enter result like 3.74 or -1.20 and it generates a Telegram image.
